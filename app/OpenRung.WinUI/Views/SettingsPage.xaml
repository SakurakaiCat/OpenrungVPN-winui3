<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenRung.WinUI.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Padding="24" Spacing="16" MaxWidth="560">
        <TextBlock FontSize="20" FontWeight="SemiBold" Text="设置" />

        <StackPanel Spacing="4">
            <TextBlock FontWeight="SemiBold" Text="捕获模式" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                TextWrapping="Wrap">
                代理模式仅接管系统代理；TUN 模式接管全设备流量，需要管理员权限。
            </TextBlock>
            <ComboBox
                x:Name="ModeCombo"
                HorizontalAlignment="Left"
                SelectionChanged="ModeCombo_SelectionChanged">
                <ComboBoxItem Content="代理模式" Tag="proxy" />
                <ComboBoxItem Content="TUN 模式" Tag="tun" />
            </ComboBox>
        </StackPanel>

        <StackPanel Spacing="4">
            <TextBlock FontWeight="SemiBold" Text="系统代理" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                TextWrapping="Wrap">
                当前系统代理：<Run FontWeight="SemiBold" Text="{x:Bind ViewModel.SystemProxyText, Mode=OneWay}" />。代理模式下内核会先接管已有代理、断开时恢复；开启自动清除后，启动时直接移除已有代理（例如其他代理工具留下的）。
            </TextBlock>
            <CheckBox
                IsChecked="{x:Bind ViewModel.AutoClearProxy, Mode=TwoWay}"
                Checked="AutoClearProxy_Checked"
                Content="启动时自动清除已有系统代理"
                Unchecked="AutoClearProxy_Unchecked" />
            <Button
                HorizontalAlignment="Left"
                Click="ClearProxy_Click"
                Content="立即清除系统代理"
                IsEnabled="{x:Bind ViewModel.NotProxyBusy, Mode=OneWay}" />
        </StackPanel>

        <StackPanel Spacing="4">
            <TextBlock FontWeight="SemiBold" Text="核心" />
            <TextBlock
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind ViewModel.CoreVersion, Mode=OneWay}" />
            <TextBlock
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind ViewModel.EngineLine, Mode=OneWay}" />
            <TextBlock
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="{x:Bind ViewModel.EndpointText, Mode=OneWay}" />
        </StackPanel>

        <StackPanel Spacing="4">
            <TextBlock FontWeight="SemiBold" Text="关于" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                TextWrapping="Wrap">
                <Run Text="OpenRung WinUI3 由 " />
                <Run FontWeight="SemiBold" Text="@SakurakaiCat" />
                <Run Text=" 基于 OpenRung 仓库二次开发。" />
            </TextBlock>
            <HyperlinkButton
                Content="OpenRung 上游仓库 · github.com/openrung/openrung"
                NavigateUri="https://github.com/openrung/openrung" />
            <HyperlinkButton
                Content="本客户端仓库 · github.com/SakurakaiCat/openrung-winui3"
                NavigateUri="https://github.com/SakurakaiCat/openrung-winui3" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}"
                TextWrapping="Wrap"
                Text="Copyright © 2026 @SakurakaiCat。本客户端基于上游 OpenRung 的修改作品，与上游 OpenRung 均以 GNU General Public License v3.0（GPL-3.0）发布，详见仓库根目录的 LICENSE 文件。" />
        </StackPanel>
    </StackPanel>
</Page>
